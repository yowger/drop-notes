import Loader from "../Loader/Loader"

export default function PageLoader() {
    return (
        <div className="h-screen w-full flex items-center p-2 justify-center ">
            <Loader />
        </div>
    )
}
